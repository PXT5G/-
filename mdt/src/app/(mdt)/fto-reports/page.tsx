import { PlaceholderModule } from "@/components/ui/PlaceholderModule";
import { messages } from "@/lib/i18n/messages";

export default function FtoReportsPage() {
  return <PlaceholderModule title={messages.nav.ftoReports} />;
}

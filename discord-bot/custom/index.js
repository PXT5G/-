/**
 * ═══════════════════════════════════════════════════════════════
 * بوت PlayStation MDT — ضع ملف بوتك هنا عندما تجلبه
 * ═══════════════════════════════════════════════════════════════
 *
 * هذا الملف لمجتمع السوني + MDT Web فقط.
 * FiveM (fivem-mdt-nui/) منفصل — لا تربطه هنا.
 */

export default {
  // async onReady({ client }) {},
  // async onDuty({ entry, discordClient }) {},
  // extendApi({ api, discordClient }) {
  //   api.get('/custom/ping', (_req, res) => res.json({ pong: true }));
  // },
};

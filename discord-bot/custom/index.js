/**
 * ═══════════════════════════════════════════════════════════════
 * ضع ملف البوت الخاص بك هنا
 * ═══════════════════════════════════════════════════════════════
 *
 * 1. انسخ منطق بوتك إلى هذا الملف
 * 2. صدّر الكائن بـ export default { ... }
 * 3. لا تشغّل client.login هنا — النظام يشغّل البوت تلقائياً
 *
 * الـ Hooks المتاحة:
 *   onReady, onInteraction, onDuty, onExport, onFine, onNotify, extendApi
 */

export default {
  // async onReady({ client }) {},
  // async onDuty({ entry, discordClient }) {},
  // extendApi({ api, discordClient }) {
  //   api.get('/custom/ping', (_req, res) => res.json({ pong: true }));
  // },
};
